export const metadata = {
  title: 'Order Confirmed | The Break Surf',
  robots: { index: false, follow: false },
};

export default function SuccessLayout({ children }: { children: React.ReactNode }) {
  return children;
}
