export const metadata = {
  title: 'Cart | The Break Surf',
  robots: { index: false, follow: false },
};

export default function CartLayout({ children }: { children: React.ReactNode }) {
  return children;
}
